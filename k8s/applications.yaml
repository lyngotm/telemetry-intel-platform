# ============================================================
# API Gateway
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: tip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: telemetry-intel-platform-api-gateway:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: tip-config
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tip-secrets
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: jwt-keys
              mountPath: /app/keys
              readOnly: true
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
      volumes:
        - name: jwt-keys
          secret:
            secretName: jwt-keys
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: tip
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080

# ============================================================
# Ingestion Consumer
# ============================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingestion-consumer
  namespace: tip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingestion-consumer
  template:
    metadata:
      labels:
        app: ingestion-consumer
    spec:
      containers:
        - name: ingestion-consumer
          image: telemetry-intel-platform-ingestion-consumer:latest
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: tip-config
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tip-secrets
                  key: POSTGRES_PASSWORD
            - name: KAFKA_CONSUMER_GROUP
              value: "ingestion-consumer-group"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ingestion-consumer
  namespace: tip
spec:
  selector:
    app: ingestion-consumer
  ports:
    - port: 9090
      targetPort: 9090

# ============================================================
# Anomaly Detection
# ============================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: anomaly-detection
  namespace: tip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: anomaly-detection
  template:
    metadata:
      labels:
        app: anomaly-detection
    spec:
      containers:
        - name: anomaly-detection
          image: telemetry-intel-platform-anomaly-detection:latest
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: tip-config
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tip-secrets
                  key: POSTGRES_PASSWORD
            - name: KAFKA_CONSUMER_GROUP
              value: "anomaly-detection-group"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: anomaly-detection
  namespace: tip
spec:
  selector:
    app: anomaly-detection
  ports:
    - port: 9091
      targetPort: 9091

# ============================================================
# Diagnosis Service
# ============================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: diagnosis-service
  namespace: tip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diagnosis-service
  template:
    metadata:
      labels:
        app: diagnosis-service
    spec:
      containers:
        - name: diagnosis-service
          image: telemetry-intel-platform-diagnosis-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 9092
          envFrom:
            - configMapRef:
                name: tip-config
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tip-secrets
                  key: POSTGRES_PASSWORD
            - name: KAFKA_CONSUMER_GROUP
              value: "diagnosis-service-group"
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws-credentials
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws-credentials
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_SESSION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: aws-credentials
                  key: AWS_SESSION_TOKEN
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: diagnosis-service
  namespace: tip
spec:
  selector:
    app: diagnosis-service
  ports:
    - port: 9092
      targetPort: 9092

# ============================================================
# HPA — API Gateway
# ============================================================
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-gateway
  namespace: tip
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-gateway
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70

